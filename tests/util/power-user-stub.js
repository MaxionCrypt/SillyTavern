// Test stub for SillyTavern's power-user.js module.
//
// Deliberately minimal: only the settings buckets Remodel's native-transport
// code reaches (sysprompt/context/instruct), as inert plain objects.

export const power_user = {
    sysprompt: {},
    context: {},
    instruct: {},
};
